sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t ssimonTW/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true ssimonTW/react-test npm test

after_success:
  - docker build -t ssimonTW/multi-client ./client
  - docker build -t ssimonTW/multi-nginx ./nginx
  - docker build -t ssimonTW/multi-server ./server
  - docker build -t ssimonTW/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push ssimonTW/multi-client
  - docker push ssimonTW/multi-nginx
  - docker push ssimonTW/multi-server
  - docker push ssimonTW/multi-worker
