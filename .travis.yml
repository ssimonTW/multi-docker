sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t ssimon/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true ssimon/react-test npm test

after_success:
  - docker build -t ssimon/multi-client ./client
  - docker build -t ssimon/multi-nginx ./nginx
  - docker build -t ssimon/multi-server ./server
  - docker build -t ssimon/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push ssimon/multi-client
  - docker push ssimon/multi-nginx
  - docker push ssimon/multi-server
  - docker push ssimon/multi-worker
